name: Bun

on:
  push:
    branches:
      - '**'
    paths:
      - 'Ticketing-Airlines-Web/**'
  pull_request:
    branches:
      - '**'
    paths:
      - 'Ticketing-Airlines-Web/**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: Ticketing-Airlines-Web

    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Build
        run: bun run build

      - name: Test
        run: bun test:unit
